package com.zahri.lighttodo.widget

/**
 * No longer used — the widget now uses fixed-row layout instead of ListView.
 * Kept as a placeholder to avoid breaking any potential references.
 */
object TodoWidgetService
